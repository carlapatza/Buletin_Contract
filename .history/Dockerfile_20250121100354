# Verwenden Sie das offizielle .NET SDK-Image als Build-Umgebung
FROM mcr.microsoft.com/dotnet/sdk:6.0 AS build-env
WORKDIR /app

# Kopieren Sie die csproj-Datei und wiederherstellen Sie die Abhängigkeiten
COPY *.csproj ./
RUN dotnet restore

# Kopieren Sie den Rest des Codes und erstellen Sie die Anwendung
COPY . ./
RUN dotnet publish -c Release -o out

# Erstellen Sie das endgültige Image
FROM mcr.microsoft.com/dotnet/aspnet:6.0
WORKDIR /app
COPY --from=build-env /app/out .

# Exponieren Sie den Port, auf dem die Anwendung läuft
EXPOSE 80

# Starten Sie die Anwendung
ENTRYPOINT ["dotnet", "YourAppName.dll"]
